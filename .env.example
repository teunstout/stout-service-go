# pgAdmin login - pgAdmin is published outside docker (5050:80), so set a real
# password here, not the placeholder below. Copy this file to .env and fill in.
PGADMIN_DEFAULT_EMAIL=admin@example.com
PGADMIN_DEFAULT_PASSWORD=change-me
